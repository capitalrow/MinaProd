apiVersion: apps/v1
kind: Deployment
metadata:
  name: mina-app
  labels:
    app: mina
spec:
  replicas: 3
  selector:
    matchLabels:
      app: mina
  template:
    metadata:
      labels:
        app: mina
    spec:
      containers:
      - name: mina
        image: mina:latest
        ports:
        - containerPort: 5000
        env:
        - name: DATABASE_URL
          valueFrom:
            secretKeyRef:
              name: mina-secrets
              key: database-url
        - name: SESSION_SECRET
          valueFrom:
            secretKeyRef:
              name: mina-secrets
              key: session-secret
        - name: OPENAI_API_KEY
          valueFrom:
            secretKeyRef:
              name: mina-secrets
              key: openai-api-key
        - name: REDIS_URL
          value: "redis://redis-service:6379/0"
        - name: FLASK_ENV
          value: "production"
        resources:
          requests:
            memory: "512Mi"
            cpu: "250m"
          limits:
            memory: "1Gi"
            cpu: "500m"
        livenessProbe:
          httpGet:
            path: /health/live
            port: 5000
          initialDelaySeconds: 30
          periodSeconds: 10
        readinessProbe:
          httpGet:
            path: /health/ready
            port: 5000
          initialDelaySeconds: 5
          periodSeconds: 5
        lifecycle:
          preStop:
            exec:
              command: ["/bin/sleep", "15"]

---
apiVersion: v1
kind: Service
metadata:
  name: mina-service
spec:
  selector:
    app: mina
  ports:
  - port: 80
    targetPort: 5000
  type: ClusterIP

---
# SECURITY CRITICAL: Replace with actual secrets using kubectl or secrets management
# kubectl create secret generic mina-secrets \
#   --from-literal=database-url="postgresql://REAL_USER:REAL_PASS@postgres-service:5432/mina_db" \
#   --from-literal=session-secret="$(openssl rand -base64 32)" \
#   --from-literal=openai-api-key="sk-YOUR_REAL_OPENAI_API_KEY"
apiVersion: v1
kind: Secret
metadata:
  name: mina-secrets
  annotations:
    description: "Production secrets - MUST be replaced with real values"
type: Opaque
# PRODUCTION DEPLOYMENT: Remove stringData section and use kubectl create secret instead
stringData:
  database-url: "REPLACE_WITH_REAL_DATABASE_URL"
  session-secret: "REPLACE_WITH_REAL_SESSION_SECRET"
  openai-api-key: "REPLACE_WITH_REAL_OPENAI_API_KEY"

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: mina-ingress
  annotations:
    nginx.ingress.kubernetes.io/rewrite-target: /
    nginx.ingress.kubernetes.io/ssl-redirect: "true"
    cert-manager.io/cluster-issuer: "letsencrypt-prod"
spec:
  tls:
  - hosts:
    - REPLACE_WITH_YOUR_DOMAIN.com
    secretName: mina-tls
  rules:
  - host: REPLACE_WITH_YOUR_DOMAIN.com
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: mina-service
            port:
              number: 80

---
apiVersion: v1
kind: ConfigMap
metadata:
  name: redis-config
data:
  redis.conf: |
    maxmemory 256mb
    maxmemory-policy allkeys-lru
    save 900 1
    save 300 10
    save 60 10000

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
spec:
  replicas: 1
  selector:
    matchLabels:
      app: redis
  template:
    metadata:
      labels:
        app: redis
    spec:
      containers:
      - name: redis
        image: redis:7-alpine
        ports:
        - containerPort: 6379
        volumeMounts:
        - name: redis-config
          mountPath: /usr/local/etc/redis/redis.conf
          subPath: redis.conf
        resources:
          requests:
            memory: "128Mi"
            cpu: "100m"
          limits:
            memory: "256Mi"
            cpu: "200m"
      volumes:
      - name: redis-config
        configMap:
          name: redis-config

---
apiVersion: v1
kind: Service
metadata:
  name: redis-service
spec:
  selector:
    app: redis
  ports:
  - port: 6379
    targetPort: 6379
  type: ClusterIP